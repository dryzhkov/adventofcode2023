import { solvePuzzle } from './index';

describe('part 1', () => {
    it('sampleData', () => {
        const answer = solvePuzzle();
        console.log(answer);
        expect(answer).toBe(answer);
    });
});
